<Page
    x:Class="ReunionSlideshow.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ReunionSlideshow"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="5*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Visibility="{x:Bind ViewModel.ControlPanelVisibility}">
                <Button Content="Set Image Folder" Command="{x:Bind ViewModel.SetImageFolderCommand}"/>
                <Button Content="Start Slideshow" Command="{x:Bind ViewModel.StartSlideShowCommand}"/>
                <Button Content="Next Image" Command="{x:Bind ViewModel.NextImageCommand}"/>
            </StackPanel>
            <Image Grid.Column="1" DataContext="{x:Bind ViewModel.ViewImage}"></Image>
        </Grid>
    </Grid>
</Page>
